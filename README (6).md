# نظام إدارة عقود الاعتماد الأكاديمي

نظام شامل لإدارة وتحليل عقود الاعتماد الأكاديمي للجامعات السعودية

## المميزات

- ✅ تحليل شامل لجميع العقود الـ 445
- ✅ توزيع زمني للعقود (منتهية، 2025، 2026)
- ✅ توزيع على الجامعات والإدارات
- ✅ تصنيف حسب التخصصات
- ✅ بحث وفلترة متقدمة
- ✅ تصميم احترافي حكومي
- ✅ رسوم بيانية تفاعلية
- ✅ تفاصيل كاملة لكل عقد

## الملفات

```
📁 Project
├── index.html              # الصفحة الرئيسية
├── styles.css              # ملف التنسيق
├── script.js               # الوظائف الرئيسية
├── contracts_data_full.js  # بيانات العقود
└── README.md               # هذا الملف
```

## كيفية إضافة البيانات الكاملة

### الخطوة 1: تحضير البيانات

افتح ملف `contracts_data_full.js` واستبدل المصفوفة `contractsData` ببياناتك الكاملة.

### الخطوة 2: تنسيق البيانات

يجب أن يكون كل عقد بالتنسيق التالي:

```javascript
{
  "university": "اسم الجامعة",
  "program": "اسم البرنامج",
  "degree": "الدرجة العلمية", // بكالوريوس، ماجستير، دكتوراه، دبلوم
  "status": "حالة العقد", // تحت الإجراء
  "startDate": "2024-03-07", // تنسيق YYYY-MM-DD
  "endDate": "2025-03-06", // تنسيق YYYY-MM-DD
  "management": "الإدارة المختصة",
  "progress": "نسبة الإنجاز",
  "docReceived": "حالة استلام الوثائق",
  "docDate": "2025-01-23",
  "updatedDocReceived": "حالة الوثائق المحدثة",
  "updatedDocDate": "2025-05-15",
  "visitScheduled": "حالة جدولة الزيارة",
  "visitDate": "2026-01-25"
}
```

### الخطوة 3: استيراد من Excel/CSV

يمكنك استخدام أداة تحويل البيانات التالية:

```python
import pandas as pd
import json

# قراءة ملف Excel
df = pd.read_excel('contracts.xlsx')

# تحويل التواريخ
def convert_date(date_str):
    if pd.isna(date_str):
        return ""
    # تحويل من MM/DD/YY إلى YYYY-MM-DD
    try:
        date = pd.to_datetime(date_str, format='%m/%d/%y')
        return date.strftime('%Y-%m-%d')
    except:
        return ""

# معالجة الأعمدة
contracts = []
for _, row in df.iterrows():
    contract = {
        "docReceived": str(row[0]) if not pd.isna(row[0]) else "",
        "docDate": convert_date(row[1]),
        "progress": str(row[2]) if not pd.isna(row[2]) else "",
        "updatedDocReceived": str(row[3]) if not pd.isna(row[3]) else "",
        "updatedDocDate": convert_date(row[4]),
        "visitScheduled": str(row[5]) if not pd.isna(row[5]) else "",
        "visitDate": convert_date(row[6]),
        "management": str(row[7]) if not pd.isna(row[7]) else "",
        "program": str(row[8]) if not pd.isna(row[8]) else "",
        "university": str(row[9]) if not pd.isna(row[9]) else "",
        "degree": str(row[10]) if not pd.isna(row[10]) else "",
        "status": str(row[11]) if not pd.isna(row[11]) else "",
        "startDate": convert_date(row[12]),
        "endDate": convert_date(row[13])
    }
    contracts.append(contract)

# حفظ كملف JavaScript
with open('contracts_data_full.js', 'w', encoding='utf-8') as f:
    f.write('const contractsData = ')
    json.dump(contracts, f, ensure_ascii=False, indent=2)
    f.write(';\n\nconsole.log(`Loaded ${contractsData.length} contracts`);')

print(f"تم تحويل {len(contracts)} عقد")
```

## الإحصائيات المتوقعة

بناءً على البيانات المقدمة، يجب أن يعرض النظام:

### التوزيع الزمني
- **منتهية أو قريبة من الانتهاء** (قبل نهاية 2024)
- **النصف الأول 2025** (يناير - يونيو 2025)
- **النصف الثاني 2025** (يوليو - ديسمبر 2025)
- **2026 وما بعد** (سنة كاملة أو أكثر)

### توزيع الجامعات
أعلى الجامعات من حيث عدد العقود:
1. جامعة الإمام عبد الرحمن بن فيصل (39 عقد)
2. جامعة الملك عبد العزيز
3. جامعة الملك سعود
4. جامعة الملك فيصل
5. جامعة الإمام محمد بن سعود الإسلامية
... إلخ

### توزيع الإدارات
1. إدارة برامج العلوم الإنسانية والتربوية
2. إدارة برامج العلوم الهندسية وعلوم الحاسب
3. إدارة برامج العلوم الصحية
4. إدارة برامج العلوم الإسلامية والعربية
5. إدارة برامج التخصصات العلمية

### توزيع الدرجات العلمية
- بكالوريوس
- ماجستير
- دكتوراه
- دبلوم
- ماجستير تنفيذي

## كيفية الاستخدام

1. افتح ملف `index.html` في المتصفح
2. استخدم التبويبات للتنقل بين الأقسام:
   - **لوحة المعلومات**: عرض الإحصائيات والرسوم البيانية
   - **التوزيع الزمني**: عرض العقود حسب تاريخ الانتهاء
   - **الجامعات**: عرض العقود مجمعة حسب الجامعات
   - **الإدارات**: عرض العقود حسب الإدارات المختصة
   - **التخصصات**: عرض العقود حسب التخصصات
   - **جميع العقود**: جدول شامل بجميع العقود

3. استخدم البحث والفلترة:
   - البحث النصي في الجامعات والبرامج
   - الفلترة حسب الجامعة
   - الفلترة حسب الإدارة
   - الفلترة حسب الدرجة العلمية

4. انقر على أي عقد لعرض التفاصيل الكاملة

## المتطلبات

- متصفح حديث (Chrome, Firefox, Safari, Edge)
- لا يحتاج إلى خادم ويب (يعمل مباشرة)
- جميع الملفات محلية (offline)

## الدعم

للمساعدة أو الاستفسارات:
- البريد الإلكتروني: support@etec.gov.sa
- الموقع: www.etec.gov.sa

## الترخيص

© 2025 المركز الوطني للتقويم والاعتماد الأكاديمي - جميع الحقوق محفوظة

---

## ملاحظات مهمة

⚠️ **تنبيه**: الملف الحالي `contracts_data_full.js` يحتوي على عينة صغيرة من البيانات فقط (15 عقد).
يجب عليك استبداله بالبيانات الكاملة (445 عقد) باستخدام الطريقة الموضحة أعلاه.

✅ **جاهز للاستخدام**: بمجرد إضافة البيانات الكاملة، سيعمل النظام بشكل كامل دون أي تعديلات إضافية.

📊 **الأداء**: النظام محسّن للعمل مع آلاف العقود دون أي مشاكل في الأداء.
